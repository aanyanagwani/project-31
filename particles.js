class particles {
    constructor(x, y) {
        var options = {
            restitution: 1,
            friction: 0,
            isStatic:true
        }
   this.color = color(random(0,255),random(0,255),random(0,255))
   World.add(world,this.color);
    }
}
display()
{
    
}
